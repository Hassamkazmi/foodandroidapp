import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ReferScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#021b17',
      }}>
      <Text>ReferScreen</Text>
    </View>
  );
};

export default ReferScreen;

const styles = StyleSheet.create({});
